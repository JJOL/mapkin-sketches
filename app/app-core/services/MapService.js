module.exports = function(nativeProvider) {
    let MapService = MapService || {};

    MapService.get = function(mapId) {

      nativeProvider.get(mapId, )


    }

    MapService.getId(user, name) {
      UserService.getUser(user, function(result) {
        var user = result.data
        user.getMaps().has
      })
    }


    return MapService;
}
