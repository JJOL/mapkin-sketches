function Map(ownerId, createdOn, points) {
  this.subscribers = [];
  this.points = [];
  this.ownerId;
  this.createdOn
}

Map.prototype.getNearPoints = function(x, y, r) {

}

Map.prototype.checkPermissionForUser = function (userId, permission) {
  return subscribers[userId].permissions[permission];
};

Map.prototype.searchPointsByName = function (queryStr) {
  points.
};
